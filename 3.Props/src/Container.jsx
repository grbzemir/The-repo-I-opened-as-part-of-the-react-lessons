import React from 'react'


// eslint-disable-next-line react/prop-types
function Container({ children }) {

    return (
        <div>
            <div>Container components calisti</div>
            {children}
        </div>
    )
}

export default Container
